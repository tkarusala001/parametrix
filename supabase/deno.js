export default {
    lint: {
      rules: {
        exclude: ["no-import-prefix", "no-unversioned-import"]
      }
    }
  };
  
  // ✅ CommonJS export
  module.exports = {
    lint: {
      rules: {
        exclude: ["no-import-prefix", "no-unversioned-import"]
      }
    }
  };